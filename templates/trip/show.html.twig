{% extends 'base.html.twig' %}

{% block title %}Trip{% endblock %}

{% block body %}
    <div id="modal1" class="modal">
        <div class="modal-content center-align">
            <h3><i class="material-icons trip-title">contact_phone</i></h3>
            <h5>{{ volunteer ? volunteer.givenName ~ ' ' ~ volunteer.familyName : user.givenName ~ ' ' ~ user.familyName }}</h5>
            <h5 class="pt bck-ice pl pb pr border-radius" id="phoneNumber">
                <i class="material-icons pr-s">phone</i>
                <a href="{{ 'tel:' ~ (volunteer ? volunteer.telephone : user.telephone) }}">{{ volunteer ? volunteer.telephone : user.telephone }}</a>
            </h5>
        </div>
        <div class="modal-footer">
            <a class="modal-close waves-effect waves-green btn-flat">Close</a>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <h3 class="bck-night white-text center pad border-radius">
                <i class="material-icons trip-title">directions_car</i>
                <i class="material-icons trip-title">map</i>
            </h3>
            <table class="table table-trip col s10 offset-s1">
                <tbody>
                <tr>
                    <th><i class="material-icons pt-s">date_range</i></th>
                    <td class="center-align">{{ trip.date|date('Y-m-d') }}</td>
                </tr>
                <tr>
                    <th><i class="material-icons pt-s">access_time</i></th>
                    <td class="center-align">{{ trip.date|date('h:i A') }}</td>
                </tr>
                <tr>
                    <th><i class="material-icons pt-s">gps_fixed</i></th>
                    <td class="center-align">{{ trip.departure.name }}</td>
                </tr>
                <tr>
                    <th><i class="material-icons pt-s">location_on</i></th>
                    <td class="center-align">{{ trip.arrival.name }}</td>
                </tr>
                {% if trip.volunteer %}
                    <tr>
                        <th><i class="material-icons pt-s">people</i></th>
                        <td class="center-align"><a class="waves-effect waves-light modal-trigger" href="#modal1">{{ volunteer.givenName }} {{ volunteer.familyName }}</a></td>
                    </tr>
                {% endif %}
                {% if app.user.status == 'volunteer' %}
                    <tr>
                        <th><i class="material-icons pt-s">people</i></th>
                        <td class="center-align"><a class="waves-effect waves-light modal-trigger" href="#modal1">{{ user.givenName }} {{ user.familyName }}</a></td>
                    </tr>
                {% endif %}
                </tbody>
            </table>
        </div>
        <div class="pt trip-show-tools">
            {% if app.user.status == 'beneficiary' %}
                {% if not trip.volunteer %}
                    <div class="center-align">
                        <a href="{{ path('trip_edit', {'id': trip.id}) }}" class="btn"><i class="material-icons">edit</i></a>
                    </div>
                {% endif %}
                <div class="center-align">
                    {{ include('trip/_delete_form.html.twig') }}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
